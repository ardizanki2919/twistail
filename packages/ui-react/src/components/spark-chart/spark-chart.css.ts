import { type VariantProps, tv } from 'tailwind-variants'

const sparkChartStyles = tv({
  base: [],
  slots: {},
  variants: {},
  defaultVariants: {},
})

type SparkChartStyles = VariantProps<typeof sparkChartStyles>

export { sparkChartStyles, type SparkChartStyles }
