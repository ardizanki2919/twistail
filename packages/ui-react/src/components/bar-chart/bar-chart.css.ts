import { type VariantProps, tv } from 'tailwind-variants'

const barChartStyles = tv({
  base: [],
  slots: {},
  variants: {},
  defaultVariants: {},
})

type BarChartStyles = VariantProps<typeof barChartStyles>

export { barChartStyles, type BarChartStyles }
