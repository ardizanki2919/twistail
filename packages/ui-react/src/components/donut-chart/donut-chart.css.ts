import { type VariantProps, tv } from 'tailwind-variants'

const donutChartStyles = tv({
  base: [],
  slots: {},
  variants: {},
  defaultVariants: {},
})

type DonutChartStyles = VariantProps<typeof donutChartStyles>

export { donutChartStyles, type DonutChartStyles }
