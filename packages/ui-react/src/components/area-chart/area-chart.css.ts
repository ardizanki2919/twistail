import { type VariantProps, tv } from 'tailwind-variants'

const areaChartStyles = tv({
  base: [],
  slots: {},
  variants: {},
  defaultVariants: {},
})

type AreaChartStyles = VariantProps<typeof areaChartStyles>

export { areaChartStyles, type AreaChartStyles }
