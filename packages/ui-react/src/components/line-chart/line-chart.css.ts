import { type VariantProps, tv } from 'tailwind-variants'

const lineChartStyles = tv({
  base: [],
  slots: {},
  variants: {},
  defaultVariants: {},
})

type LineChartStyles = VariantProps<typeof lineChartStyles>

export { lineChartStyles, type LineChartStyles }
