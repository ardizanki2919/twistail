import { type VariantProps, tv } from 'tailwind-variants'

const comboChartStyles = tv({
  base: [],
  slots: {},
  variants: {},
  defaultVariants: {},
})

type ComboChartStyles = VariantProps<typeof comboChartStyles>

export { comboChartStyles, type ComboChartStyles }
