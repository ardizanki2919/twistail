import { type VariantProps, tv } from 'tailwind-variants'

const categoryBarStyles = tv({
  base: [],
  slots: {},
  variants: {},
  defaultVariants: {},
})

type CategoryBarStyles = VariantProps<typeof categoryBarStyles>

export { categoryBarStyles, type CategoryBarStyles }
