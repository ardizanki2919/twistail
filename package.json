{
    "name": "repo",
    "private": true,
    "scripts": {
        "dev": "turbo dev --filter=!example-*",
        "build": "turbo build --filter=!example-*",
        "start": "turbo start --filter=!example-*",
        "storybook": "turbo storybook --filter=twistail-react",
        "cleanup": "turbo cleanup && pnpm dlx rimraf pnpm-lock.yaml node_modules",
        "update-deps": "npm-check-updates --configFileName .ncurc.json",
        "publish:dry": "turbo publish:dry --filter=!example-*",
        "filter": "pnpm --silent --filter",
        "format": "biome format . --write",
        "lint": "turbo lint",
        "check": "turbo check",
        "typecheck": "turbo typecheck"
    },
    "devDependencies": {
        "@biomejs/biome": "catalog:",
        "npm-check-updates": "^17.1.15",
        "turbo": "^2.4.4"
    },
    "packageManager": "pnpm@10.6.1",
    "pnpm": {
        "onlyBuiltDependencies": ["@biomejs/biome", "@parcel/watcher", "esbuild"]
    }
}
